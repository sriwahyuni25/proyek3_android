import 'package:flutter/material.dart';

import '../../theme.dart';

class KonsultasiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Konsultasi Page',
      style: secondaryTextStyle,
      ),
    );
  }
}